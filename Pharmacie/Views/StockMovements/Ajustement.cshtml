@model StockAjustementViewModel
@{
    ViewData["Title"] = "Ajustement de stock";
}

<div class="page-header">
    <h1 class="page-title">Ajustement de stock</h1>
    <p class="page-lead mb-0">Corrige la quantité d’un lot (inventaire, erreur). Utilisez une variation positive ou négative.</p>
</div>

<div class="row">
    <div class="col-lg-8">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white py-3 border-bottom d-flex align-items-center gap-2">
                <i class="bi bi-sliders text-primary fs-5" aria-hidden="true"></i>
                <span class="fw-semibold">Ajustement</span>
            </div>
            <div class="card-body p-3 p-md-4">
                <form asp-action="Ajustement">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="mb-3">
                        <label asp-for="BatchId" class="form-label"></label>
                        <select asp-for="BatchId" class="form-select" asp-items="@((SelectList)ViewData["BatchId"]!)">
                            <option value="">— Choisir un lot —</option>
                        </select>
                        <span asp-validation-for="BatchId" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Delta" class="form-label"></label>
                        <input asp-for="Delta" class="form-control" type="number" />
                        <span asp-validation-for="Delta" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Reason" class="form-label"></label>
                        <input asp-for="Reason" class="form-control" placeholder="ex. Inventaire physique…" />
                        <span asp-validation-for="Reason" class="text-danger"></span>
                    </div>
                    <div class="d-flex flex-wrap gap-2 pt-2 border-top">
                        <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg me-1" aria-hidden="true"></i>Enregistrer l’ajustement</button>
                        <a class="btn btn-outline-secondary" asp-action="Index">Annuler</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
